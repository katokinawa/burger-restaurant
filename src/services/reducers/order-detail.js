const initialState = {
  data: [
    {
      _id: '0'
    }
  ]

};

export const orderDetail = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
}
